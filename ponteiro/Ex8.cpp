#include <stdio.h>

struct TpPont
{
	int chave;
	char nome[20];
	TpPont *ant, *prox;
};

TpPont Inserir(TpPont *lista)
{
	
	
	return lista;
}

int main(void)
{
	TpPont *lista = NULL;
	
	lista = Inserir(lista);
	
	
	return 0;	
}
